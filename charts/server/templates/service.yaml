apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.name | default "rclone-server" }}
  labels:
    app: {{ .Values.name | default "rclone-server" }}
  {{- with .Values.service.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    app: {{ .Values.name | default "rclone-server" }}